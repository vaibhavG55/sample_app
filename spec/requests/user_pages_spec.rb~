require 'spec_helper'

describe "UserPages" do

  subject{page}
  
  describe "signup page" do
     
     before{visit signup_path}
     
     it{should heave_selector('h1',text: 'Sign Up')}
     it{should heave_selector('title',text: full_title('Sign Up'))}
  end
end
